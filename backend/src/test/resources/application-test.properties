spring.datasource.url=${TEST_DB_URL:jdbc:mysql://localhost:3306/job_scheduler}
spring.datasource.username=${TEST_DB_USERNAME:root}
spring.datasource.password=${TEST_DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

jwt.secret=${TEST_JWT_SECRET:TestOnlyJwtSecretKeyForDistributedJobScheduler2026SecureHmacKey123456789}
jwt.expiration=${TEST_JWT_EXPIRATION:86400000}